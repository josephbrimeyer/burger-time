<li>
    {{name}}

    <button class="devour-burger" data-id="{{id}}" data-newdevour="{{devour}}">
        {{burger_name}} {{#if devour}} - Want to devour!{{else}} {{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE</button>
</li>